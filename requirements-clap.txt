# Optional CLAP backend dependencies.
# Install with:
#   pip install -r requirements.txt -r requirements-clap.txt
# Or via pyproject.toml extra:
#   pip install -e .[clap]
#
# #423 reproducibility matrix. Keep these exact pins in sync with pyproject.toml.
torch==2.13.0
transformers==5.16.1
